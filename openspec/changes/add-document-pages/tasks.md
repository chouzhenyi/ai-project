## 1. 路由与菜单配置

- [ ] 1.1 在 `src/router/index.tsx` 中新增 `/document` 和 `/document/form/:id?` 两条懒加载路由
- [ ] 1.2 在 `menuItems` 中新增"单据管理"菜单项，active 匹配 `/document` 前缀

## 2. 单据列表页

- [ ] 2.1 创建 `src/views/document/index.tsx`，导出默认组件
- [ ] 2.2 定义列表页 EasyTable 的 `columns` 配置（单据编号、名称、供应商、类型、金额、状态、创建时间）
- [ ] 2.3 实现搜索栏（单据编号/名称输入框 + 搜索按钮）
- [ ] 2.4 实现分页功能
- [ ] 2.5 实现操作列（编辑导航到 `/document/form/:id`，删除带确认弹窗）
- [ ] 2.6 实现"新建单据"按钮，导航到 `/document/form`
- [ ] 2.7 添加 loading、empty 状态处理

## 3. 单据新建/编辑页

- [ ] 3.1 创建 `src/views/document/Form.tsx`，导出默认组件
- [ ] 3.2 通过 `useParams` 获取 `:id`，判断新建/编辑模式，设置页面标题
- [ ] 3.3 实现"基本信息"Tab 的 EasyForm schema（单据编号、名称、类型、日期、金额、币种、经办人、部门、备注、状态）
- [ ] 3.4 实现"发票信息"Tab 的 EasyForm schema（发票类型、号码、开票日期、金额、税率、税额、不含税金额、开票方、备注）
- [ ] 3.5 实现"配件信息"Tab，使用 EasyTable 可编辑子表格（配件名称、规格型号、单位、数量、单价、金额）
- [ ] 3.6 实现"设备信息"Tab 的 EasyForm schema（设备名称、类型、规格型号、序列号、数量、单价、金额、状态）
- [ ] 3.7 实现"供应商信息"Tab 的 EasyForm schema（供应商名称、联系人、电话、地址、开户银行、银行账号、税号）
- [ ] 3.8 实现"附件信息"Tab，使用 Upload 组件展示附件上传区域
- [ ] 3.9 实现"关联单据信息"Tab，使用 EasyTable 展示关联单据列表（关联单据编号、类型、名称、日期、关联人）
- [ ] 3.10 实现表单验证与提交逻辑，新建时追加数据、编辑时更新数据
- [ ] 3.11 编辑模式下加载已有单据数据并预填充所有 Tab

## 4. 联调与收尾

- [ ] 4.1 确保列表页操作（新建/编辑/删除）与表单页数据流转正确
- [ ] 4.2 运行 `pnpm lint` 检查代码规范
- [ ] 4.3 运行 `pnpm build` 确保 TypeScript 类型检查和构建通过
