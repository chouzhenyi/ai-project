## 1. 路由与菜单配置

- [x] 1.1 在 `src/router/index.tsx` 中新增 `/document` 和 `/document/form/:id?` 两条懒加载路由
- [x] 1.2 在 `menuItems` 中新增"单据管理"菜单项，active 匹配 `/document` 前缀

## 2. 单据列表页

- [x] 2.1 创建 `src/views/document/index.tsx`，导出默认组件
- [x] 2.2 定义列表页 EasyTable 的 `columns` 配置（单据编号、名称、供应商、类型、金额、状态、创建时间）
- [x] 2.3 实现搜索栏（单据编号/名称输入框 + 搜索按钮）
- [x] 2.4 实现分页功能
- [x] 2.5 实现操作列（编辑导航到 `/document/form/:id`，删除带确认弹窗）
- [x] 2.6 实现"新建单据"按钮，导航到 `/document/form`
- [x] 2.7 添加 loading、empty 状态处理

## 3. 单据新建/编辑页

- [x] 3.1 创建 `src/views/document/Form.tsx`，导出默认组件
- [x] 3.2 通过 `useParams` 获取 `:id`，判断新建/编辑模式，设置页面标题
- [x] 3.3 实现页面顶部操作栏模块：保存、提交审批、创建报账单、创建付款单、废弃五个按钮（保存执行数据保存逻辑，提交审批保存并改变状态为"已提交审批"，创建报账单/创建付款单弹出提示（Demo），废弃需确认弹窗并改变状态为"已废弃"）
- [x] 3.4 实现"基本信息"模块的 EasyForm schema（单据编号、名称、类型、日期、金额、币种、经办人、部门、备注、状态），使用 Card 包裹
- [x] 3.4 实现"发票信息"模块：Card 包裹，EasyTable 表格展示发票列表（发票类型、号码、开票日期、金额、税率、税额、不含税金额、开票方、备注 + 右侧操作列），顶部 buttonGroup 新建按钮，Drawer 抽屉内 EasyForm 新建/编辑发票数据
- [x] 3.5 实现"配件信息"模块：Card 包裹，EasyTable 表格展示配件列表（配件名称、规格型号、单位、数量、单价、金额 + 右侧操作列），顶部 buttonGroup 新建按钮，Drawer 抽屉内 EasyForm 新建/编辑配件数据
- [x] 3.6 实现"设备信息"模块：Card 包裹，EasyTable 表格展示设备列表（设备名称、类型、规格型号、序列号、数量、单价、金额、状态 + 右侧操作列），顶部 buttonGroup 新建按钮，Drawer 抽屉内 EasyForm 新建/编辑设备数据
- [x] 3.7 实现"供应商信息"模块：Card 包裹，EasyTable 表格展示供应商列表（供应商名称、联系人、电话、地址、开户银行、银行账号、税号 + 右侧操作列），顶部 buttonGroup 新建按钮，Drawer 抽屉内 EasyForm 新建/编辑供应商数据
- [x] 3.8 实现"附件信息"模块：Card 包裹，EasyTable 表格展示附件列表（附件名称、附件大小、上传日期 + 右侧操作列），顶部 buttonGroup 新建按钮，Drawer 抽屉内 Upload 组件 + EasyForm 新建/编辑附件数据
- [x] 3.9 实现"关联单据信息"模块：Card 包裹，EasyTable 表格展示关联单据列表（关联单据编号、类型、名称、日期、关联人 + 右侧操作列），顶部 buttonGroup 新建按钮，Drawer 抽屉内 EasyForm 新建/编辑关联单据数据
- [x] 3.10 实现各模块的 Drawer 新建/编辑交互逻辑（点击新建/编辑打开抽屉，提交后回写表格数据，关闭抽屉）
- [x] 3.11 实现表单验证与提交逻辑，新建时追加数据、编辑时更新数据
- [x] 3.12 编辑模式下加载已有单据数据并预填充基本信息模块及所有表格模块数据

## 4. 联调与收尾

- [x] 4.1 确保列表页操作（新建/编辑/删除）与表单页数据流转正确
- [x] 4.2 运行 `pnpm lint` 检查代码规范
- [x] 4.3 运行 `pnpm build` 确保 TypeScript 类型检查和构建通过
